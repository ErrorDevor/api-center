import { Metadata } from "next";

export const metadata: Metadata = {
   title: "API - BEST AI PRICE – Compare AI API Prices Across 100+ Providers",
};

export default function HomeRouteLayout({ children }: { children: React.ReactNode }) {
   return children;
}
